# --- App Info ---
spring.application.name=wims

# --- PostgreSQL Database Settings ---
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.wyiownvewbxtppogzovq
spring.datasource.password=WimsRoot123!
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Server ---
server.port=8080
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true
server.servlet.session.timeout=60m
server.servlet.session.persistent=true

# Optional: enable debug logging for session management
logging.level.org.springframework.security.web.session=DEBUG

# --- Spring Session Settings ---
spring.session.timeout=30m
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=never
spring.session.jdbc.table-name=SPRING_SESSION
